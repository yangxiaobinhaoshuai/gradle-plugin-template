enableFeaturePreview('VERSION_CATALOGS')

apply from: "$rootDir.parentFile/gradle/globalDefinitions.gradle"

dependencyResolutionManagement {
    versionCatalogs {
        libDeps {
            //from(files("../gradle/libs.versions.toml"))
            from(files(versions_toml))
        }
    }
}
